---
import Section from '../ui/Section.astro';
import Container from '../ui/Container.astro';
import SectionHeading from '../ui/SectionHeading.astro';

interface Props {
  value: {
    heading: string;
    lead: string;
    bullets: { title: string; description: string }[];
  };
}

const { value } = Astro.props satisfies Props;
---

<Section id="value" background="tint">
  <Container class="grid gap-12 lg:grid-cols-[1.1fr_1fr] lg:items-center">
    <div>
      <SectionHeading
        eyebrow="BLUEHORIZON EDGE"
        title={value.heading}
        description={value.lead}
      />
    </div>
    <div class="grid gap-6">
      {value.bullets.map((bullet) => (
        <div class="rounded-3xl border border-ocean-100/70 bg-white/90 p-8 shadow-sm shadow-ocean-100/50">
          <h3 class="font-display text-xl font-semibold text-deep">{bullet.title}</h3>
          <p class="mt-3 text-base text-deep/70">{bullet.description}</p>
        </div>
      ))}
    </div>
  </Container>
</Section>
