---
import Container from '../ui/Container.astro';

interface Props {
  footer: {
    tagline: string;
    officesHeading: string;
    offices: { city: string; address: string }[];
    contactHeading: string;
    contact: { phone: string; email: string };
    socialHeading: string;
    socials: { label: string; href: string }[];
    legal: string;
  };
}

const { footer } = Astro.props satisfies Props;
---

<footer class="border-t border-ocean-100 bg-white text-deep">
  <Container class="grid gap-12 py-16 md:grid-cols-4">
    <div class="md:col-span-2">
      <div class="flex items-center gap-4">
        <div class="flex h-11 w-11 items-center justify-center rounded-full bg-ocean-500 font-display text-xl font-semibold text-white">
          BH
        </div>
        <div>
          <p class="font-display text-xl font-semibold text-deep">BlueHorizon</p>
          <p class="text-xs uppercase tracking-[0.25em] text-deep/50">Maritime &amp; Docks</p>
        </div>
      </div>
      <p class="mt-6 max-w-md text-sm text-deep/60">{footer.tagline}</p>
    </div>
    <div>
      <h4 class="text-xs font-semibold uppercase tracking-[0.25em] text-deep/50">{footer.officesHeading}</h4>
      <ul class="mt-4 space-y-3 text-sm text-deep/70">
        {footer.offices.map((office) => (
          <li>
            <p class="font-semibold text-deep">{office.city}</p>
            <p>{office.address}</p>
          </li>
        ))}
      </ul>
    </div>
    <div class="space-y-8">
      <div>
        <h4 class="text-xs font-semibold uppercase tracking-[0.25em] text-deep/50">{footer.contactHeading}</h4>
        <div class="mt-4 space-y-2 text-sm text-deep/70">
          <a class="block transition hover:text-ocean-600" href={`tel:${footer.contact.phone.replace(/[^+\d]/g, '')}`}>
            {footer.contact.phone}
          </a>
          <a class="block transition hover:text-ocean-600" href={`mailto:${footer.contact.email}`}>
            {footer.contact.email}
          </a>
        </div>
      </div>
      <div>
        <h4 class="text-xs font-semibold uppercase tracking-[0.25em] text-deep/50">{footer.socialHeading}</h4>
        <ul class="mt-4 flex flex-wrap gap-3 text-sm">
          {footer.socials.map((social) => (
            <li>
              <a
                class="rounded-full border border-ocean-100 px-3 py-1.5 text-deep/70 transition hover:border-ocean-200 hover:text-ocean-600"
                href={social.href}
                target="_blank"
                rel="noopener noreferrer"
              >
                {social.label}
              </a>
            </li>
          ))}
        </ul>
      </div>
    </div>
  </Container>
  <div class="border-t border-ocean-100">
    <Container class="py-6 text-center text-xs uppercase tracking-[0.2em] text-deep/40">
      {footer.legal}
    </Container>
  </div>
</footer>
